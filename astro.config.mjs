import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';

const BASE_URL = import.meta.env.BASE_URL;

// https://astro.build/config
export default defineConfig({
	site: 'https://martinristovski.github.io',
	base: '/martin.shoes',
	integrations: [
		starlight({
			title: 'martin.shoes',
			social: {
				discord: 'https://www.stitchdown.com/join-stitchdown-premium/',
			},
			sidebar: [
				{
					label: 'Home',
					link: BASE_URL + '/',
				},
				{
					label: 'Introduction',
					autogenerated: { directory: 'introduction' },
					items: [
						// Each item here is one entry in the navigation menu.
						{ label: 'Brannock', link: BASE_URL + '/introduction/brannock/' },
					],
				},
				{
					label: 'Makers',
					autogenerate: { directory: 'makers' },
				},
				{
					label: 'Places',
					autogenerate: { directory: 'places' },
				}
			],
		}),
	],
});
